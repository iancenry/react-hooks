import { useState } from 'react';
import Login from './Login';
import User from './User';

const UseContext = () => {
  return (
    <div>
      <Login /> <User />
    </div>
  );
};

export default UseContext;
